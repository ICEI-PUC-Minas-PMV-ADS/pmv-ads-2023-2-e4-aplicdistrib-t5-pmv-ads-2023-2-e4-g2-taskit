"use client";
import styled from "styled-components";

export const TaskContainer = styled.div`
  flex: 1;
  display: flex;
  flex-direction: column;
  width: 80vw;
  gap: 4rem;

  > main {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 3rem;
  }
`;

